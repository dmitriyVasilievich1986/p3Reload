/**
 * Type definitions for stats-based availability rules.
 */

/** Comparison operators supported when gating events by a social stat threshold. */
export type Operators = 'gt' | 'ge' | 'eq' | 'lt' | 'le';
